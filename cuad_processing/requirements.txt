
# Requirements for CUAD Processing Tools

# Core requirements for OSS120 Bedrock Labeler
boto3>=1.28.0
pyyaml>=6.0

# Optional: For enhanced CSV processing
pandas>=1.5.0

# Optional: For progress bars in batch processing
tqdm>=4.65.0

# Optional: For LangGraph-based agentic classifier with reflection pattern
# Install with: pip install langgraph
# langgraph>=0.0.20
# Note: LangGraph requires additional dependencies (langchain-core, etc.)
# These will be installed automatically when you install langgraph
